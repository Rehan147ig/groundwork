{
  "name": "groundwork",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/console"
  ],
  "scripts": {
    "dev": "npm run dev --workspace apps/console",
    "build": "npm run build --workspace apps/console",
    "lint": "npm run lint --workspace apps/console",
    "test:ingestion": "python -m unittest discover services/ingestion/tests"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0"
  }
}
